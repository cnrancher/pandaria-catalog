apiVersion: v1
kind: Service
metadata:
  name: alertmanager-receiver
spec:
  selector:
    app: alertmanager-receiver
  ports:
    - protocol: TCP
      port: {{ .Values.port }}
      targetPort: 9094